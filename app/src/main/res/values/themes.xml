<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Yportfolio" parent="android:Theme.Material.Light.NoActionBar" />

     <!-- Splash Screen API -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FDFBFF</item> <!-- Couleur de fond -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Yportfolio</item>
    </style>
</resources>